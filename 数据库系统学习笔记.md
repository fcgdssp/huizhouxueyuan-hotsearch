# 数据库系统学习笔记

> 用「惠院热搜」这个真实项目，学大学《数据库系统》这门课。
> 每个概念都按「是什么 → 在我们项目哪里 → 动手练习」来讲。
> 建议打开 server.js 对照着看。

---

## 第一章 关系模型：表、行、列、键

### 1.1 关系（Relation）= 表

数据库里说的「关系」就是一张二维表。我们项目里有 4 张关系（表）：

| 表名 | 干什么用 |
|---|---|
| items | 热搜（每条热搜一行）|
| messages | 访客留言 |
| users | 管理员账号 |
| sessions | 登录会话（谁登录了）|

### 1.2 元组（Tuple）= 行，属性（Attribute）= 列

- 一行 = 一条记录 = 一个「元组」。比如 items 表里「计科院新生报到流程全攻略」这一行，就是一个元组。
- 一列 = 一个字段 = 一个「属性」。比如 title 是属性，它的「值域」（能取的值）是文字。

### 1.3 键（Key）

- **候选键**：能唯一确定一行的一列（或几列）。
- **主键（Primary Key）**：从候选键里选一个做「唯一标识」。我们每张表的 `id` 都是主键，sessions 表用 `token` 做主键。
- **外键（Foreign Key）**：一张表里存了另一张表的主键，用来表示「关联」。

看代码：server.js 里 `CREATE TABLE items (... id INTEGER PRIMARY KEY ...)` —— 这句就声明了主键。

### 练习

打开 server.js，找出 4 张表各自的**主键**是哪一列，说说为什么选它。

---

## 第二章 SQL：增删改查

SQL 分两类：

- **DDL（数据定义语言）**：建表、改表、删表。如 `CREATE TABLE`、`ALTER TABLE`。
- **DML（数据操作语言）**：对数据的增删改查。

### 2.1 查（SELECT）—— 最常用

```sql
SELECT * FROM items;                              -- 查全部
SELECT * FROM items WHERE cat = '学习';            -- 只查学习分类
SELECT * FROM items ORDER BY sort_order, id;       -- 按顺序排
```

我们项目里几乎每个接口都在用 SELECT。看 server.js 的 `GET /api/items`。

### 2.2 增（INSERT）

```sql
INSERT INTO items (cat, title, ...) VALUES ('学习', '新热搜', ...);
```

看 server.js 里「热搜：新增」那段。

### 2.3 改（UPDATE）

```sql
UPDATE items SET clicks = clicks + 1 WHERE id = 204;
```

看「热搜：点击量 +1」那段，就是典型的 UPDATE。

### 2.4 删（DELETE）

```sql
DELETE FROM items WHERE id = 204;
```

### 练习

在后台随便增一条热搜、改一下、删掉，然后去 server.js 里找到对应的 `INSERT / UPDATE / DELETE` 语句，对号入座。

---

## 第三章 完整性约束

数据库用「约束」保证数据不会乱。

### 3.1 主键约束

`id INTEGER PRIMARY KEY` —— 保证每行 id 不重复、不为空。

### 3.2 唯一约束（UNIQUE）

`username TEXT NOT NULL UNIQUE` —— 保证管理员账号不重复。这就是「账号已存在」报错的底层原因。

### 3.3 外键约束（我们项目缺这个，正好学）

`sessions` 表里有个 `user_id`，它应该对应 `users` 表的 `id`——这就是「外键」的**逻辑关系**。但我们建表时**没写 FOREIGN KEY 约束**，所以数据库本身不会强制校验。

> 概念：外键约束 = 数据库强制保证「sessions.user_id 必须是 users 里真实存在的 id」。

### 练习

给 sessions 表补一句外键约束，让它引用 users 表。想想：这样做了之后，如果删掉一个管理员，他的 session 会怎样？（这是后面「事务」和「级联删除」的引子。）

---

## 第四章 数据库设计：范式

「范式」解决的是「表设计得好不好、有没有数据冗余」的问题。核心几个：

- **1NF**：每个属性不可再分（都是原子值）。
- **2NF**：消除「部分依赖」。
- **3NF**：消除「传递依赖」（非主键列不能依赖另一个非主键列）。

我们的表基本都在 3NF：比如 items 表里每条热搜的所有信息都只依赖「这一条热搜本身」，没有"同一信息存两处"的冗余。

### 练习

想一想：如果当初把「管理员用户名」直接写进 messages 表（留言里存"谁处理的"），会不会有冗余？为什么用外键 user_id 更好？

---

## 第五章 事务（ACID）

「事务」= 一组操作，要么**全部成功**，要么**全部失败**（不能只做一半）。

我们的项目里有个典型场景：**删除管理员时，要同时删掉他的 session**（server.js 里 `DELETE /api/users/:id` 做了两步）。

这两步应该是「原子」的：如果删了用户、但删 session 失败了，就会留下"没主人的 session"。

ACID 四个特性：
- **A 原子性**：要么全做，要么全不做。
- **C 一致性**：数据始终满足约束。
- **I 隔离性**：多个操作同时进行互不干扰。
- **D 持久性**：成功后数据不丢。

### 练习

指出 server.js 里「删除管理员」那两步操作为什么最好用事务包起来。（进阶：用 SQLite 的 `BEGIN / COMMIT / ROLLBACK` 试试把它改成事务。）

---

## 第六章 进阶：索引、并发、安全（了解即可）

- **索引**：给常用的查询列加索引能加速（如给 items.cat 加索引）。我们数据少，暂时用不上。
- **并发**：多个请求同时读写数据库时的冲突处理。
- **安全/权限**：我们项目里的 `role` 字段 + `requireSuper` 中间件，就是最基础的一层「权限控制」——不同角色能做的操作不同。

---

## 附录：我们项目的 4 张表

**items（热搜）**：id、cat、title、tuan、mark、badge、badgeColor、views、label、labelColor、extra、link、detail、clicks、sort_order

**messages（留言）**：id、name、content、created_at、status

**users（管理员）**：id、username（唯一）、password_hash、role、created_at

**sessions（会话）**：token（主键）、user_id（外键，指向 users.id）、created_at

---

> 用法建议：先看第一章，然后随时在聊天里问我「XX 是什么意思」，我会结合 server.js 的真实代码给你讲；每章的「练习」你做完可以发我，我帮你检查。
