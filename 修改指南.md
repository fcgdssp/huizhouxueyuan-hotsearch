# 惠院热搜系统 · 使用说明

这是一份零基础说明，告诉你怎么启动、怎么用后台管理热搜和留言。不需要你会写代码。

## 一、这是什么

一个「前端 + 后端 + 数据库」的小系统，用来给院团委办公室做招新宣传。

- 热搜内容存在数据库里（一个叫 data.db 的文件）。
- 改热搜不用改代码，在后台网页上点几下就行。
- 还带两个功能：统计每条热搜被点了多少次、收集访客留言。

## 二、怎么启动

每次要用之前，做这两步：

1. 打开终端（PowerShell 或命令行），进入文件夹 `D:\Users\84930\Desktop\招新网页`
2. 运行：

```
node server.js
```

看到「惠院热搜后端已启动」就是成功了。这个窗口别关，关了就停了。

第一次启动前需要先运行一次 `npm install`（装依赖），如果之前已经装过就不用再装。

## 三、怎么访问

启动后，用浏览器打开这两个地址：

- 主页面（访客看到的）：`http://localhost:3000`
- 后台（你管理用的）：`http://localhost:3000/admin.html`

## 四、主页面能做什么（访客视角）

- 顶部四个分类标签，点击切换。
- 点热搜词条：进详情页；带外链的词条会新窗口打开外部链接。
- 点顶部「爆」按钮：进「团委办公室」聚合页。
- 点顶部「留言」按钮：填表提交留言。
- 点空白处：放烟花特效。

## 五、后台能做什么（管理视角）

打开 `http://localhost:3000/admin.html`，有两大块：

1. 热搜管理：
   - 新增、编辑、删除热搜；
   - 顶部搜索框按标题关键词搜、下拉按分类筛；
   - 每条后面有「置顶 / 上移 / 下移」按钮，调整顺序（主页面刷新后排名跟着变，置顶的排第一）；
   - 显示「点击量」。
2. 留言反馈：
   - 查看、删除留言；
   - 每条可标记「已处理 / 未处理」，顶部显示未处理条数。

操作完，主页面刷新一下就能看到变化。

## 六、后台新增/编辑热搜时，每个字段是什么意思

| 字段 | 含义 | 怎么填 |
|---|---|---|
| 分类 | 属于哪个标签 | 新生 / 学习 / 工作 / 生活 |
| 标题 | 热搜显示的标题 | 必填 |
| 团办相关 | 是不是团委办公室相关 | 选「是」会进顶部「爆」聚合页 |
| 爆/升标志 | 右侧的小标志 | 无 / 爆 / 升 |
| 小标签 badge | 标题后的小字标签 | 如「热」「新」「官宣」，可空 |
| badge 颜色 | 小标签颜色 | 默认 / 橙 / 蓝 |
| 浏览量 | 标题下显示的数字（纯文字） | 如 236000，可空 |
| meta 小字 label | 标题下的小字 | 一般可空 |
| label 颜色 | 那个小字颜色 | 默认 / 橙 / 蓝 |
| 右上角文字 extra | 条目右上角 | 一般可空 |
| 外链 link | 点这条热搜跳转的外部网址 | 如微信推文链接，可空 |
| 详情正文 | 点进词条后看到的内容 | 换行用 <br> |

「点击量」不用你填，是系统自动统计的（访客每点一次加 1）。「排序顺序」也不用你填，由每条后面的「置顶 / 上移 / 下移」按钮控制。

## 七、文件夹里这些文件是干嘛的

| 文件 / 文件夹 | 作用 |
|---|---|
| server.js | 后端程序，跑起来当服务器，管数据库和接口 |
| data.db | 数据库文件，存热搜和留言（自动生成） |
| package.json | 项目配置，记录用了哪些依赖 |
| node_modules | 下载好的依赖库（Express 等），别手动改 |
| public/index.html | 主页面（访客看到的那个） |
| public/admin.html | 管理后台 |
| cyl.html / zhu.html | 以前的纯静态版本，已经不用了，留作参照，别改 |

## 八、常见问题

**报错 EADDRINUSE（端口被占用）**
说明已经有一个 `node server.js` 在跑了。二选一：直接用已经在跑的那个（开浏览器访问 localhost:3000 即可），或者在终端按 Ctrl+C 停掉旧的，再重新运行。

**想让数据恢复初始状态**
删掉 `data.db` 文件，再运行 `node server.js`，会自动重新导入 25 条初始热搜。

**改了后台，主页面没变化**
刷新一下主页面。

## 九、想给别人访问（上线）

现在 `localhost` 只能你自己电脑访问。想让别人在浏览器打开，需要把系统部署到云平台（会得到一个公网网址）。这步比较麻烦，等本地用熟了、想上线了，再一步步做。

## 十、改烟花效果（可选）

烟花特效的代码在主页面 `public/index.html` 里，搜索「烟花特效」那段。要调颜色、大小、光尾长短，都在那里改，改完刷新主页面看效果。
